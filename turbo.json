{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"typecheck": {
			"dependsOn": ["^typecheck"],
			"inputs": ["**/*.{ts,tsx}"]
		},
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["**/*.{ts,tsx}"]
		}
	}
}
